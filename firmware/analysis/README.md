# Firmware analysis outputs

These are derived from the original Intel HEX and may be regenerated.

The flat `firmware.bin` produced during early analysis is intentionally not treated as an address-authoritative representation; Intel HEX address information is preserved in the original image and address-aware dumps.
