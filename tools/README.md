# Tools

## Debian dependencies

```bash
sudo apt install gputils srecord binutils file python3-serial
```

## Safety

The serial monitor is read-only by design. Do not add arbitrary `CW` fuzzing to the live-stove workflow.
